<div class="row">
    <div class="col-lg-12">
        <h1>Contact Me</h1>
        <%= form_tag(pages_contact_path, method: "get", remote: true) do %>
          <div id="flash"></div>
          <%= label_tag(:message, "Your email:") %>
          <%= email_field(:user, :message) %>
          <%= submit_tag "Send" %>
        <% end %>
    </div>
</div>
